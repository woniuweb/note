
## 10.16.28.24

### root

#### Nginx（V3R6）

```bash
/usr/local/nginxV3R6/nginx/sbin/nginx -c /usr/local/nginxV3R6/nginx/conf/nginx.conf
```

#### Tomcat（BI，7005）

```bash
/usr/local/tomcat/tomcat-app-bi-7005/bin/startup.sh
/data/tomcat/tomcat-app-bi-7005/bin/startup.sh
```

#### Tomcat（应用开发，8888）

```bash
~~/usr/local/tomcat832/tomcat-ys-app-dev-8888/bin/startup.sh~~
```

> 当前发现同一目录有两个进程（PID 6351、8738），需确认是否重复启动。

#### Tomcat（应用开发，9998）

```bash
/data/tomcat/tomcat-ys-app-dev-9998/bin/startup.sh
```

#### Redis（6379）

```bash
/usr/local/redis/bin/redis-server /usr/local/redis/etc/redis_6379.conf
```

#### Redis（6380）

```bash
/usr/local/redis-7.4.6/bin/redis-server /usr/local/redis-7.4.6/etc/redis_6380.conf
```

### yyuser

#### Nginx

```bash
/usr/local/nginx/sbin/nginx -c /usr/local/nginx/conf/nginx.conf
```

#### Eureka 注册中心（8766）

```bash
java -jar /usr/local/eureka-server-8766/app/eureka-server.jar --spring.config.location=/usr/local/eureka-server-8766/conf/application.yml --logging.path=/usr/local/eureka-server-8766/logs
```

## 10.16.23.199

### root

#### Tomcat（应用开发，8082）

```bash
/home/yyuser/tomcat-ys-app-dev-8082/bin/startup.sh
```

应用配置：`/home/yyuser/tomcat-ys-app-dev-8082/webapps/conf/application.properties`。

#### Nginx（8082）

```bash
/usr/local/nginx/sbin/nginx -c /usr/local/nginx/conf/nginx-8082.conf
```

#### Redis（6887）

已确认二进制和数据目录：`/usr/local/redis-7.4.6/bin/redis-server`、`/usr/local/redis-7.4.6/bin`。

启动配置文件无法从在线进程唯一还原，暂不记录可能错误的启动命令。

### weblogic1

#### 自动部署服务

```bash
java -jar /data/npcs/autodeploy/auto-deploy-system-1.0.0.jar
```

#### ~~Nginx~~

~~原启动命令：~~

```bash
/data/npcs/nginx/sbin/nginx -c /data/npcs/nginx/conf/nginx.conf
```

~~`/data/npcs/nginx/sbin/nginx` 和工作目录均已删除；进程仍在运行，但停止后不能按此命令重启，需先恢复 Nginx 文件。~~

## 10.16.22.52

### yyuser

#### 语音识别服务（10089）

部署目录：`/data/yyuser/asr2.0/g_dolphin`

```bash
cd /data/yyuser/asr2.0/g_dolphin
./start.sh start
```

停止服务：

```bash
cd /data/yyuser/asr2.0/g_dolphin
./start.sh stop
```
